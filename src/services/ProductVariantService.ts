import type { Prisma } from '@prisma/client'; import { notFound } from '../errors/AppError'; import type { AuthUser } from '../types/auth'; import { assertPermission } from '../utils/permissions'; import { buildPageMeta, resolveOrderBy, toSkipTake } from '../utils/pagination'; import type { IProductVariantRepository } from '../repositories/ProductVariantRepository'; import type { CreateProductVariantInput, ProductVariantListQuery, UpdateProductVariantInput } from '../validators/productVariant.schemas';
export class ProductVariantService { constructor(private readonly repo:IProductVariantRepository){} async list(user:AuthUser,q:ProductVariantListQuery){assertPermission(user,'catalog.view');const p=toSkipTake(q);const [items,total]=await this.repo.list({companyId:user.companyId,branchId:q.branchId??user.branchId,...(q.productId?{productId:q.productId}:{}),...(q.isActive===undefined?{}:{isActive:q.isActive}),...(q.search?{OR:['name','sku','barcode'].map(f=>({[f]:{contains:q.search,mode:'insensitive'}}))}:{})},p.skip,p.take,resolveOrderBy(q.sortBy,q.sortOrder,['name','sku','createdAt']) as Prisma.ProductVariantOrderByWithRelationInput);return {items,meta:buildPageMeta(total,q.page,q.pageSize)}} async get(user:AuthUser,id:string){assertPermission(user,'catalog.view');const x=await this.repo.findById(user.companyId,id);if(!x)throw notFound('Product variant not found');return x} async create(user:AuthUser,input:CreateProductVariantInput){assertPermission(user,'catalog.manage');const {productId,...data}=input;return this.repo.create({...data,product:{connect:{id:productId}},company:{connect:{id:user.companyId}},branch:{connect:{id:user.branchId}},createdBy:user.id,updatedBy:user.id,attributeValues:data.attributeValues as Prisma.InputJsonValue|undefined})} async update(user:AuthUser,id:string,input:UpdateProductVariantInput){assertPermission(user,'catalog.manage');await this.get(user,id);return this.repo.update(id,{...input,attributeValues:input.attributeValues as Prisma.InputJsonValue|undefined,updatedBy:user.id})} async remove(user:AuthUser,id:string){assertPermission(user,'catalog.manage');await this.get(user,id);return this.repo.softDelete(id,user.id)} }
